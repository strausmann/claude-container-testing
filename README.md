# claude-container-testing

Run a repo's tests/lint/build in a reproducible, toolchain-decoupled dev container. This Claude Code plugin provides commands and skills for seamless integration with your codebase's test and build workflows.

**Requires:** Docker + `@devcontainers/cli` (global, pinned)

**Documentation:** not yet — filled in Task 12
